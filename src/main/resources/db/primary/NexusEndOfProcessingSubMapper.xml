<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nexus.NexusEndOfMonthProcessingBatch.domain.primary.mapper.NexusEndOfProcessingSubMapper">
    <resultMap id="NexusEndOfProcessingSubResultMap" type="com.nexus.NexusEndOfMonthProcessingBatch.domain.primary.entity.NexusEndOfProcessingSubEntity">
        <result column="parent_id" property="parentId"/>
        <result column="month_and_day_of_uses" property="monthAndDayOfUses" typeHandler="com.nexus.NexusEndOfMonthProcessingBatch.domain.type_handler.LocalDateTypeHandler"/>
        <result column="half_time_use" property="halfTimeUse"/>
        <result column="registered_user_id" property="registeredUserId"/>
        <result column="registered_date" property="registeredDate" typeHandler="com.nexus.NexusEndOfMonthProcessingBatch.domain.type_handler.LocalDateTimeTypeHandler"/>
        <result column="update_user_id" property="updateUserId"/>
        <result column="update_date" property="updateDate" typeHandler="com.nexus.NexusEndOfMonthProcessingBatch.domain.type_handler.LocalDateTimeTypeHandler"/>
    </resultMap>

    <insert id="insert" parameterType="hashmap">
        INSERT INTO nexus_end_of_processing_sub (

        parent_id,
        month_and_day_of_uses,
        half_time_use,
        registered_user_id,
        registered_date

        ) VALUES
        <foreach collection="entities" item="entity" open="(" separator=") , (" close=")">
            ${entity.parentId},
            #{entity.monthAndDayOfUses},
            #{entity.halfTimeUse},
            #{entity.registeredUserId},
            now()
        </foreach>
    </insert>

    <delete id="deleteByParentId" parameterType="hashmap">
        DELETE FROM nexus_end_of_processing_sub
        <where>
            parent_id = ${parentId}
        </where>
    </delete>

    <select id="findByParentId" parameterType="hashmap" resultMap="NexusEndOfProcessingSubResultMap">
        SELECT * FROM nexus_end_of_processing_sub
        <where>
            parent_id = ${parentId}
        </where>
    </select>

    <select id="findByParentIds" parameterType="hashmap" resultMap="NexusEndOfProcessingSubResultMap">
        SELECT * FROM nexus_end_of_processing_sub
        <where>
            parent_id IN
            <foreach collection="parentIds" item="parentId" open="(" separator="," close=")">
                ${parentId}
            </foreach>
        </where>
    </select>

</mapper>